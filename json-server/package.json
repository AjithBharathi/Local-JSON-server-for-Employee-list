{
  "name": "json-server",
  "version": "1.0.0",
  "description": "json-server(backend) for our employee list project. the file employee_database.json used to store our employee list as json format",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
